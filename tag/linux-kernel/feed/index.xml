<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	>

<channel>
	<title>Linux kernel &#8211; Just a collection of various tech blabs</title>
	<atom:link href="/tag/linux-kernel/feed/" rel="self" type="application/rss+xml" />
	<link>/</link>
	<description>Cyber, Linux, ML, and more</description>
	<lastBuildDate>Sun, 02 Jan 2022 23:38:25 +0000</lastBuildDate>
	<language>en-US</language>
	<sy:updatePeriod>
	hourly	</sy:updatePeriod>
	<sy:updateFrequency>
	1	</sy:updateFrequency>
	<generator>https://wordpress.org/?v=5.8.2</generator>

<image>
	<url>/wp-content/uploads/2021/12/cropped-cropped-cropped-pinguin-32x32.png</url>
	<title>Linux kernel &#8211; Just a collection of various tech blabs</title>
	<link>/</link>
	<width>32</width>
	<height>32</height>
</image> 
	<item>
		<title>Committing code to the Linux Kernel &#8211; from a stuck process to a git commit</title>
		<link>/2022/01/02/committing-code-to-the-linux-kernel-from-a-stuck-process-to-a-fix-in-2-days/</link>
					<comments>/2022/01/02/committing-code-to-the-linux-kernel-from-a-stuck-process-to-a-fix-in-2-days/#respond</comments>
		
		<dc:creator><![CDATA[xrl1]]></dc:creator>
		<pubDate>Sun, 02 Jan 2022 23:31:28 +0000</pubDate>
				<category><![CDATA[Linux]]></category>
		<category><![CDATA[Commit Patch]]></category>
		<category><![CDATA[Linux kernel]]></category>
		<guid isPermaLink="false">/?p=57</guid>

					<description><![CDATA[Earlier in 2021, about March, my own code got into the Linux kernel!Long story short,&#8230;]]></description>
										<content:encoded><![CDATA[
<p>Earlier in 2021, about March, my own code got into the Linux kernel!<br>Long story short, I found a bug in some binary execution handling component called <code>binfmt_misc</code>, got to the bottom of it and fixed it, suggested a patch, and it got applied <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f642.png" alt="🙂" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>



<p>You can check out the commit here:</p>



<p><a href="https://github.com/torvalds/linux/commit/e7850f4d844e0acfac7e570af611d89deade3146">https://github.com/torvalds/linux/commit/e7850f4d844e0acfac7e570af611d89deade3146</a></p>



<p>As a big Linux enthusiast, you can just imagine how excited I&#8217;ve been when I saw this:</p>



<figure class="wp-block-image size-full"><img loading="lazy" width="355" height="36" src="/wp-content/uploads/2021/12/image.png" alt="" class="wp-image-58" srcset="/wp-content/uploads/2021/12/image.png 355w, /wp-content/uploads/2021/12/image-300x30.png 300w" sizes="(max-width: 355px) 100vw, 355px" /></figure>



<p>Now I can retire in peace.</p>



<p class="has-large-font-size" id="Long-story">The long story:</p>



<p><br>I have been working on an educational Linux challenge, and I was trying to understand better the kernel component <code>binfmt_misc</code> for a specific level I had in mind.</p>



<p>For a brief explanation on the component,  here is a it&#8217;s <a href="https://en.wikipedia.org/wiki/Binfmt_misc">Wikipedia page</a>:</p>



<p class="has-text-align-center"><em>&#8220;</em><strong> </strong><strong>binfmt_misc</strong>&nbsp;(<em>Miscellaneous Binary Format</em>) is a capability of the&nbsp;<a href="https://en.wikipedia.org/wiki/Linux_kernel">Linux kernel</a>&nbsp;which allows arbitrary&nbsp;<a href="https://en.wikipedia.org/wiki/Executable_file_format">executable file formats</a>&nbsp;to be recognized and passed to certain&nbsp;<a href="https://en.wikipedia.org/wiki/User_space">user space</a>&nbsp;applications, such as&nbsp;<a href="https://en.wikipedia.org/wiki/Emulator">emulators</a>&nbsp;and&nbsp;<a href="https://en.wikipedia.org/wiki/Virtual_machine">virtual machines</a>.<sup><a href="https://en.wikipedia.org/wiki/Binfmt_misc#cite_note-1">[1]</a></sup>&nbsp;It is one of a number of binary format handlers in the kernel that are involved in preparing a user-space program to run.<em>&#8220;</em></p>



<p>So this component allows any program to register a custom binary handler, according to some file name extension or file header magic the program defines.<br>Then, upon the execution of the binary, the <code>binfmt_misc</code> component will identify the defined program and will pass it the handling of the execution, instead of the more commonly known ELF handler or bash script handler.</p>



<p>Ok, so I was messing around with registering a new binary handler, by echoing some input into a file named <code>register</code> in the binfmt filesystem but for some reason.. the process was stuck and didn&#8217;t return. I tried to terminate it brutally with &#8220;kill -9&#8221;, but to my surprise it didn&#8217;t respond! A system reboot was necessary to recover this process <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f61b.png" alt="😛" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>



<p>This is my input in bash:</p>



<pre class="wp-block-preformatted">echo ":iiiii:E::ii::/proc/sys/fs/binfmt_misc/bla:F" &gt; /proc/sys/fs/binfmt_misc/register</pre>



<p>As you can see, the kill signal is sent <strong>successfully</strong>, but no response from bash (from which I executed the &#8216;echo&#8217; builtin command)</p>



<figure class="wp-block-image size-full is-resized"><img loading="lazy" src="/wp-content/uploads/2022/01/image-1.png" alt="" class="wp-image-62" width="664" height="161" srcset="/wp-content/uploads/2022/01/image-1.png 793w, /wp-content/uploads/2022/01/image-1-300x73.png 300w, /wp-content/uploads/2022/01/image-1-768x187.png 768w" sizes="(max-width: 664px) 100vw, 664px" /><figcaption>The immortal bash</figcaption></figure>



<p>Alrighttt, some interesting behavior from the OS, don&#8217;t you think?</p>



<p class="has-medium-font-size">Let&#8217;s get digging</p>



<p>/<code>proc/sys/fs/binfmt_misc/register</code> isn&#8217;t a standard file in the filesystem.<br>It is actually a mount point (mounted by default by many standard Linux distributions):</p>



<figure class="wp-block-image size-full"><img loading="lazy" width="845" height="51" src="/wp-content/uploads/2022/01/image-2.png" alt="" class="wp-image-63" srcset="/wp-content/uploads/2022/01/image-2.png 845w, /wp-content/uploads/2022/01/image-2-300x18.png 300w, /wp-content/uploads/2022/01/image-2-768x46.png 768w" sizes="(max-width: 845px) 100vw, 845px" /><figcaption>binfmt mount point</figcaption></figure>



<p>Then binfmt_misc is also.. a filesystem! Or more accurately, a pseudo-filesystem, which implements an interface to communicate with the binfmt kernel component from user-space <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f642.png" alt="🙂" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>



<p>Ok, so what is the <code>register</code> file and what is the string I just echoed means?<br>From a <a href="https://www.kernel.org/doc/html/latest/admin-guide/binfmt-misc.html">documentation </a>written by kernel.org: </p>



<p class="has-text-align-center">&#8220;To actually register a new binary type, you have to set up a string looking like&nbsp;<code>:name:type:offset:magic:mask:interpreter:flags</code>&nbsp;(where you can choose the&nbsp;<code>:</code>&nbsp;upon your needs) and echo it to&nbsp;<code>/proc/sys/fs/binfmt_misc/register</code>.&#8221;</p>



<p>In my input to the file, the name, type, offset, magic, and mask are non-important, therefore we will take a look at how the interpreter &#8211; &#8220;bla&#8221;, a non-existent file in the pseudo-filesystem directory, and flags &#8211; &#8216;F&#8217; (fix binary, more on that later) are all together causing the process freeze</p>



<p class="has-medium-font-size">Glasses on &#8211; reading the kernel code</p>



<p>The code behind the binfmt_misc filesystem resides in the kernel source code under &#8220;fs/binfmt_misc.c&#8221;. We can manually locate it because &#8220;fs&#8221; is the folder where most filesystem code resides <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f600.png" alt="😀" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>



<p>Then, we can find the <code>file_operations</code> struct that is assigned to the file &#8220;register&#8221;:<br></p>



<pre class="wp-block-code"><code>static const struct file_operations bm_register_operations = {
	.write		= bm_register_write,
	.llseek		= noop_llseek,
};</code></pre>



<p>So according to the above struct, each time a &#8220;write&#8221; operation is called on the file, the function <code>bm_register_write</code> will be called.</p>



<p>After reading the function&#8217;s <a href="https://github.com/torvalds/linux/blob/2347961b11d4079deace3c81dceed460c08a8fc1/fs/binfmt_misc.c#L644">code</a>, there is nothing unusual at a first glance.</p>



<p>Now let&#8217;s spawn a kernel debuger, and find out what happens inside the kernel (there are a lot of great guild on how to do this, I used kdbg on qemu with buildroot).<br>Then we find the task (thread in the kernel) in which bash is stuck, and inspect it&#8217;s backtrace:</p>



<pre class="wp-block-code has-primary-color has-text-color" style="font-size:10px"><code>0  schedule () at ./arch/x86/include/asm/current.h:15
1  0xffffffff81b51237 in rwsem_down_read_slowpath (sem=0xffff888003b202e0, count=&lt;optimized out&gt;, state=state@entry=2) at kernel/locking/rwsem.c:992
2  0xffffffff81b5150a in __down_read_common (state=2, sem=&lt;optimized out&gt;) at kernel/locking/rwsem.c:1213
3  __down_read (sem=&lt;optimized out&gt;) at kernel/locking/rwsem.c:1222
4  down_read (sem=&lt;optimized out&gt;) at kernel/locking/rwsem.c:1355
5  0xffffffff811ee22a in inode_lock_shared (inode=&lt;optimized out&gt;) at ./include/linux/fs.h:783
6  open_last_lookups (op=0xffffc9000022fe34, file=0xffff888004098600, nd=0xffffc9000022fd10) at fs/namei.c:3177
7  path_openat (nd=nd@entry=0xffffc9000022fd10, op=op@entry=0xffffc9000022fe34, flags=flags@entry=65) at fs/namei.c:3366
8  0xffffffff811efe1c in do_filp_open (dfd=&lt;optimized out&gt;, pathname=pathname@entry=0xffff8880031b9000, op=op@entry=0xffffc9000022fe34) at fs/namei.c:3396
9  0xffffffff811e493f in do_open_execat (fd=fd@entry=-100, name=name@entry=0xffff8880031b9000, flags=&lt;optimized out&gt;, flags@entry=0) at fs/exec.c:913
10 0xffffffff811e4a92 in open_exec (name=&lt;optimized out&gt;) at fs/exec.c:948
11 0xffffffff8124aa84 in bm_register_write (file=&lt;optimized out&gt;, buffer=&lt;optimized out&gt;, count=19, ppos=&lt;optimized out&gt;) at fs/binfmt_misc.c:682
12 0xffffffff811decd2 in vfs_write (file=file@entry=0xffff888004098500, buf=buf@entry=0xa758d0 ":iiiii:E::ii::i:CF
", count=count@entry=19, pos=pos@entry=0xffffc9000022ff10) at fs/read_write.c:603
13 0xffffffff811defda in ksys_write (fd=&lt;optimized out&gt;, buf=0xa758d0 ":iiiii:E::ii::i:CF
", count=19) at fs/read_write.c:658
14 0xffffffff81b49813 in do_syscall_64 (nr=&lt;optimized out&gt;, regs=0xffffc9000022ff58) at arch/x86/entry/common.c:46
15 0xffffffff81c0007c in entry_SYSCALL_64 () at arch/x86/entry/entry_64.S:120</code></pre>



<p>We see that indeed the code is stuck in <code>binfmt_register_write</code> (11) when calling the function <code>open_exec</code> (10).<br>Looks like down the road a shared lock is taken, probably on the file passed to <code>open_exec</code> (I verified it later), but the lock can&#8217;t be obtained, therefore the process waits.</p>



<p>So who takes a write lock in a manner that causes an actual deadlock? <br>We should get back to <code>bm_register_write</code> (only relevant parts):</p>



<pre class="EnlighterJSRAW" data-enlighter-language="c" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">static ssize_t bm_register_write(struct file *file, const char __user *buffer,
                   size_t count, loff_t *ppos)
{
    Node *e;
    struct inode *inode;
    struct super_block *sb = file_inode(file)->i_sb;
    struct dentry *root = sb->s_root, *dentry;
    int err = 0;

    e = create_entry(buffer, count); (1)
    ...
    inode_lock(d_inode(root)); (2)
    ...
    if (e->flags &amp; MISC_FMT_OPEN_FILE) { (3)
        struct file *f;

        f = open_exec(e->interpreter); (4)
        if (IS_ERR(f)) {
            err = PTR_ERR(f);
            pr_notice("register: failed to install interpreter file %s\n", e->interpreter); (5)
            simple_release_fs(&amp;bm_mnt, &amp;entry_count);
            iput(inode);
            inode = NULL;
            goto out2;
        }
        e->interp_file = f;
    }

    ...

    err = 0;
out2:
    dput(dentry);
out: (6)
    inode_unlock(d_inode(root));

    ...
}</pre>



<p>In (1), the entry is created, and the flags are populated.</p>



<p>In (2), a lock is taken on the whole filesystem root (!!!!). Alright, maybe this is it?</p>



<p>In (3) , we check the flags we provided in our input<br>We can see in some inner <a href="https://github.com/torvalds/linux/blob/2347961b11d4079deace3c81dceed460c08a8fc1/fs/binfmt_misc.c#L256">code</a> (called somewhere inside <code>create_entry</code>) that &#8216;F&#8217; adds the flag <code>MISC_FMT_OPEN_FILE</code>, hence the &#8220;if&#8221; statement is true and we reach the code in (4), which is the only statement that calls <code>open_exec</code> in our code flow.</p>



<p>We call <code>open_exec</code> with an interpreter path inside the filesystem root, and even though the file does not exist, an inner code will try to acquire a read lock, but after a write lock is already locking it, the code will&#8230; wait.</p>



<p>We can be sure by running <code>dmesg</code> and observing that the message in (5) has never been reached.</p>



<p>In (6), the write lock is taken down, which is too late: the task is now waiting inside <code>open_exec</code> and the function will never return</p>



<p>In short, a write lock is taken on the filesystem root, and with a specific input we cause the same task to try and read in the same place before the lock is taken down, and a deadlock transpires</p>



<p class="has-medium-font-size">Gloves on &#8211; Patching the code</p>



<p>You can see my final patch at the <a href="https://github.com/torvalds/linux/commit/e7850f4d844e0acfac7e570af611d89deade3146#diff-bf2e758056c3a407da85096cc54172c2f8ea3d3e252a6692934d494f30cc5198">kernel&#8217;s Github</a>, which is generally very simple &#8211; I just moved the &#8220;if&#8221; block before the code locks the entire kernel, and some small variable definition and freeing accordingly.</p>



<p>Then I did the tiresome work to create the patch conforming&nbsp;to the guidelines and sent it as an email to the Linux Kernel Mailing List, and.. nothing.</p>



<p>It was the end of December 2020, and I thought that maybe the holidays are causing a delay, but January came by, and my mail wasn&#8217;t even acked by anyone.<br>I sent another email, but still nothing&#8230;</p>



<p>Each section in the Kernel code has a dedicated maintainer, but the maintainer of <code>binfmt_misc.c</code> just didn&#8217;t reply <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f937-200d-2642-fe0f.png" alt="🤷‍♂️" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>



<p>I continued with my life, and then, in February 2021, I noticed that someone else has inserted a commit into <code>binfmt_misc</code>.c !<br>I emailed the guy (‫deller@gmx.de‬‏) personally and asked for help, and he pointed out that this code section isn&#8217;t really maintained and I should address a guy named Andrew Morton, who picks up random patches and forwards them to Linus.</p>



<p>Deller gave me a small fix to the code and forwarded it to Andrew with an ack.</p>



<p>Then after a couple of days, and after hopping through many different git repositories (the Linux kernel code process is very complicated) my code got into the kernel, and acked by the one and only, Linus Torvalds!</p>



<p>The public email exchange can be found in the Linux Kernel Mailing List archive starting <a href="https://lkml.org/lkml/2020/12/24/121">here</a></p>



<p>But retirement is too early, so I hope this is not my last patch <img src="https://s.w.org/images/core/emoji/13.1.0/72x72/1f609.png" alt="😉" class="wp-smiley" style="height: 1em; max-height: 1em;" /></p>
]]></content:encoded>
					
					<wfw:commentRss>/2022/01/02/committing-code-to-the-linux-kernel-from-a-stuck-process-to-a-fix-in-2-days/feed/</wfw:commentRss>
			<slash:comments>0</slash:comments>
		
		
			</item>
	</channel>
</rss>
